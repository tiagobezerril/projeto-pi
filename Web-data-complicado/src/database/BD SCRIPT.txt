SELECT s.idsensor as 'ID Sensor',
  da.porcentagem AS 'Porcentagem',
  da.dtdecoleta as 'Data de Coleta'
FROM sensor as s 
JOIN dados as da ON idsensor = fksensor 
WHERE idsensor = 2009;

SELECT em.idCadastro as 'Id Empresa',
em.razao_social as 'Razão Social',
em.nome_fantasia as 'Nome Fantasia',
en.Estado,
  en.cidade AS Cidade,
  en.cep as CEP
FROM empresa as em 
JOIN endereco as en ON idCadastro = fkempresa;
